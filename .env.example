# ── Coveo (search engine) ──────────────────────────────────────────
# Client-side: exposed to browser for search queries
NEXT_PUBLIC_COVEO_ORG_ID=your-org-id
NEXT_PUBLIC_COVEO_SEARCH_TOKEN=your-search-api-key

# Server-side: Push API for indexing/deleting documents
COVEO_ORG_ID=your-org-id
COVEO_API_KEY=your-push-api-key
COVEO_SOURCE_ID=your-push-source-id

# ── Bright Data (LinkedIn scraping) ───────────────────────────────
BRIGHT_DATA_API_TOKEN=your-bright-data-token

# ── Authentication (optional) ─────────────────────────────────────
# Protects LinkedIn import/delete endpoints. If not set, these routes are open.
ADMIN_TOKEN=choose-a-strong-random-token
NEXT_PUBLIC_ADMIN_TOKEN=choose-a-strong-random-token  # same value, exposed to client
